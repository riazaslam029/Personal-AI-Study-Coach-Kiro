# TODO: Implement in task 3.x — task CRUD and computed fields
# Functions to implement:
#   create_task(db, course_id, data) -> Task
#   get_task(db, task_id, user_id) -> Task
#   list_tasks(db, course_id, filters) -> list[Task]
#   update_task(db, task_id, data) -> Task
#   delete_task(db, task_id) -> None
#   reorder_tasks(db, course_id, task_ids) -> None
